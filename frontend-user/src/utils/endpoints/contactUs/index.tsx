import { EndPoint } from "@/types/index";
const contactUsEndpoints: EndPoint = {
  contactUs: {
    uri: "/contactUs",
    method: "POST",
    version: "/api",
  },
};
export default contactUsEndpoints;
